# Core FastAPI stack
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
aiosqlite==0.19.0
pymysql==1.1.0

# Machine Learning (torch installed separately as CPU-only wheel)
scikit-learn==1.3.2
xgboost==2.0.3
lightgbm==4.1.0
numpy==1.24.3
pandas==2.1.3
scipy==1.11.4
joblib==1.3.2

# Online Learning
river==0.21.1

# Explainability
shap==0.43.0

# Packet capture (scapy is self-contained; pcapy / pyshark are excluded
# because they require external binaries or are hard to build on Linux)
scapy==2.5.0

# Data / serialisation
pyarrow==14.0.1
pyyaml==6.0.1

# HTTP + async utils
requests==2.31.0
aiofiles==23.2.1
httpx==0.25.2

# Visualisation (used by report_service)
matplotlib==3.8.2
seaborn==0.13.0
